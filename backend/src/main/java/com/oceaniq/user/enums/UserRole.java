package com.oceaniq.user.enums;

/**
 * Enum representing user roles in the system
 * - USER: platform user with standard permissions (e.g. access to platform features)
 * - ADMIN: administrator with further permissions (e.g. managing users)
*/
public enum UserRole {
    USER,
    ADMIN
}